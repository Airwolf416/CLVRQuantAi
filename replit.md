# CLVRQuantAI

## Overview
CLVRQuantAI is a luxury, mobile-first market intelligence dashboard for real-time analysis across crypto, equities, commodities, and forex. It provides AI-driven market insights, a macro calendar, personalized alerts, and a morning brief to support informed trading decisions. The project aims to transition from AI-originated signals to a deterministic quant scorer, with AI serving a veto-only role, enhancing signal reliability and precision.

## User Preferences
- I prefer simple language.
- I like functional programming.
- I want iterative development.
- Ask before making major changes.
- I prefer detailed explanations.
- Do not make changes to `package.json`, `vite.config.ts`, `server/vite.ts`, `drizzle.config.ts`.
- Do not use raw `pg` on Node – Drizzle only.
- `CLAUDE_MODEL` stays `claude-sonnet-4-6`.
- Use `packager_install_tool` for any package install (Python or Node).

## System Architecture
The application features an Express (TypeScript) backend and a React (JSX) frontend, served via Vite on a single port. Data is persisted in PostgreSQL using Drizzle ORM. A Python FastAPI quant microservice handles deterministic quantitative scoring. The UI/UX employs a navy and gold theme with Playfair Display and IBM Plex Mono fonts.

**Key Architectural Components & Features:**
- **Quant Microservice:** A Python FastAPI service (`quant/`) generates deterministic quantitative scores, including modules for scoring, risk management (stop-loss, sizing), regime detection, GARCH modeling, and microstructure analysis.
- **Node-Python Bridge:** `server/quantClient.ts` facilitates communication between the Node.js backend and the Python quant microservice.
- **Real-time Data Aggregation:** Aggregates 1-minute OHLCV bars from sources like Hyperliquid, Binance, and Yahoo Finance.
- **Signal Generation & Hardening:** Signals originate from the quant service, are subject to cost/EV gates, and an AI veto. Signal hardening rules (e.g., `MIN_RR_AFTER_FRICTION`) optimize throughput and reduce noise.
- **Statistical Brain:** An empirical edge engine (`server/lib/statisticalBrain.ts`) analyzes historical trade data to compute win rates, expected returns, and resolution times per (token, direction) combo. It derives strict limits for Take Profits, Stop Losses, and Kill Clocks, issuing verdicts like SUPPRESS, CAUTION, NORMAL, or PREFERRED. This brain context is integrated into the AI's prompts and can veto signals.
- **Chart Vision:** `server/lib/chartRenderer.ts` generates SVG candlestick charts with technical overlays (EMAs, S/R, entry zones), converts them to PNG, and sends them as multimodal input to Claude for visual analysis validation.
- **Enhanced AI Analysis (`/api/ai/analyze`):** Integrates the Statistical Brain, Chart Vision, and intraday Execution Levels (VWAP, ORH, ORL) into the AI's context for a comprehensive analysis of user-specified tickers.
- **Daily Brief Integration:** The Statistical Brain context is used to bias trade idea selection in the daily email brief and Telegram posts, favoring empirically winning setups and avoiding suppressed ones.
- **Trade Idea Asset Class Selector:** UI allows filtering trade ideas by asset class (All, Crypto, Equities, Commodities, Forex), dynamically adjusting market types (PERP/SPOT) and filtering signals accordingly.
- **Telegram Autoposter:** A unified helper (`server/lib/buildEnrichedReasoning.ts`) creates consistent, branded captions for live signals and daily brief trade ideas, posted via a dedicated webhook. Includes admin tools for testing posts.
- **Email & Update Management:** Admin tools for email diagnostics and an `UpdateLogManager` for curating and AI-generating weekly project updates. Promo expiry reminders are managed with idempotency to prevent duplicate sends.
- **Improvement Log Mirroring:** Agent's `logImprovement()` mirrors entries to a production database for centralized tracking via HMAC-SHA256 signed HTTP.
- **Session Overlays:** Per-session VWAP (with ±1σ bands) and Opening Range (ORH/ORL) overlays are displayed on live charts for spot equities, FX, and commodities, and provided to the AI Analyst.
- **Signal Engine v1 Upgrade (Phase 1 — prompt-only, behind `PROMPT_V2_MODE`):** The v1 Signal Engine spec (Regime Gate as HARD filter, Dual-Score output `direction_probability` + `conviction` with per-asset-class thresholds, Volatility-Percentile-Adjusted R:R, Meta-label → Kelly scaling, and crypto microstructure features CVD / OBI / IV-RV) is encoded as a single `SIGNAL_ENGINE_V1` constant in `server/prompts/shared.ts` and injected into the v2 signal-generation system prompt at `server/prompts/signalGen.ts` between the killswitch / calibration block and the `RESPONSE FORMAT` line. `TradePlanSchema` is extended with optional fields `signal_status`, `regime`, `direction_probability`, `conviction`, `p_loss_meta`, `vol_percentile`, `rr_multiplier`, `kelly_fraction_applied`, `microstructure { cvd_state, obi, ivrv_spread }`, `gates_passed`, and `no_signal_reason` (all optional for backward compatibility — legacy v2 outputs that pre-date the upgrade still validate). New gate reason codes (`regime_chop`, `regime_mismatch`, `below_thresholds`, `cvd_contradict`, `ivrv_block`) are appended to `NO_TRADE_REASONS`. The prompt instructs Claude to double-write NO_SIGNAL outcomes as `direction = "NO_TRADE"` with the gate name pushed into both `kill_switches_triggered` (legacy consumers) and `no_signal_reason` (new consumers). The server-pinned `kelly_fraction` remains the BASE Kelly; `kelly_fraction_applied` documents the after-meta-shrinkage value. **Phase 2.1 — Regime Gate migrated to Python (DONE):** `quant/regime.py` rewritten to the spec's 5-state classifier (`TREND_UP / TREND_DOWN / RANGE / HIGH_VOL / CHOP` — uppercase, with directional trend split), using `percentile_rank(ATR(14), 90)` + `ADX(14)` + `percentile_rank(BB_width(20,2), 90)` + EMA20/50/200 alignment. New helpers in `quant/indicators.py`: `ema()`, `bb_width()`, `percentile_rank()`. New `regime_allows(regime, side, signal_type)` function encodes the spec's allowed-types matrix and emits `regime_chop` / `regime_mismatch` codes. `quant/scorer.py` now classifies `signal_type` (`momentum` vs `mean_reversion`) from weighted factor magnitudes (`|W_MOMENTUM*mom|` vs `|W_MEANREV*mr|`). `quant/main.py` calls `regime_allows()` after composite scoring and computes a canonical `no_signal_reason` from `gates_failed` using a priority list (regime → thresholds → liquidity → micro → R:R), mapping the legacy `z_threshold` reason to the spec name `below_thresholds`. `quant/models.py` `ScoreResponse` extended with optional `signal_type` and `no_signal_reason` fields. `server/quantClient.ts` `QuantScoreResponse` interface updated to the 5-state uppercase enum + the two new optional fields. `server/prompts/shared.ts` `SIGNAL_ENGINE_V1` §1 now opens with a SCORER-AUTHORITATIVE block: when a `SCORER PREPASS` line is present in the user prompt, the AI defers to the scorer's regime/signal_type rather than recomputing. `server/prompts/signalGen.ts` accepts a new optional `quantPrepass` input and emits the `SCORER PREPASS` line when supplied. Live HTTP integration test against `/quant/score` confirmed: BTC returns `regime: TREND_DOWN`, `signal_type: momentum`, `no_signal_reason: below_thresholds`, with the legacy `gates_failed: ["z_threshold"]` still emitted alongside for backward compat. **Phase 2.1 final-wiring fix (DONE):** the V2 shadow-mode IIFE in `server/routes.ts` (the `/api/quant` endpoint, around the `runSignalGenV2` call) now actually calls `quantScore()` before invoking the V2 prompt builder, builds a `quantPrepass` from the scorer's `regime` / `signal_type` / `no_signal_reason`, and passes it through. Without this, Phase 2.1's `quantPrepass` plumbing was code-complete but never reached — the AI kept recomputing regime in-prompt. The prepass fetch is wrapped in its own try/catch so a quant-service hiccup omits the `SCORER PREPASS:` line and the prompt falls back to in-prompt regime math (same legacy behavior, zero risk to the shadow run). End-to-end test confirmed live: BTC → scorer returns `TREND_DOWN/momentum/below_thresholds` → user prompt now contains `SCORER PREPASS: regime=TREND_DOWN, signal_type=momentum, scorer_no_signal_reason=below_thresholds`, and SIGNAL_ENGINE_V1 §1's SCORER-AUTHORITATIVE block instructs the AI to defer to it. Follow-up patch: the prepass call now derives `asset_class` from the route's known `cls` (`equity / commodity / fx / crypto`) — `normalizeAssetClass(cls === "crypto" ? undefined : cls, ticker)` — so commodities (XAU/WTI/BRENT) and FX (EURUSD/etc.) reach the scorer with `METAL` / `FOREX` instead of falling through to `STOCK`. The crypto branch deliberately passes `undefined` so the symbol heuristic still promotes BTC → `"BTC"` and ETH → `"ETH"` (otherwise the raw `"CRYPTO"` enum lumps them into `MID_CAP_DEFAULT`). Smoke-tested across all 4 classes plus BTC/ETH/SOL: all 6 resolutions correct, live BTC + XAU scorer calls both succeed. **Phase 2.2 — Dual Score migrated to Python (DONE):** `quant/scorer.py` adds `compute_dual_score(comp, gates_failed_so_far, regime_allowed, df_len)` returning deterministic `direction_probability = clip(0.50 + 0.30*tanh(|directional_z|/3.0), 0.50, 0.85)` where `directional_z = W_MOMENTUM*momentum + W_MEANREV*meanrev + W_FLOW*flow + W_SENTIMENT*sentiment` (per spec §2 "PURELY directional features" — `carry` and `volgate` are excluded since carry is a positional cost and volgate is a regime modifier; mean_reversion stays in because it predicts an opposing-direction move). The 0.30 multiplier with 3.0 divisor is sized so the formula has meaningful range up to its 0.85 ceiling (asymptote ~0.80) and discriminates: |dz|=0 → 0.50, |dz|=1 → 0.597, |dz|=2 → 0.675, |dz|=5 → 0.779. `conviction = clip(0.40 + 0.25*alignment + 0.15*regime_fit + 0.10*no_conflicts + 0.10*data_freshness, 0.40, 0.95)` where `alignment = |composite_z| / sum(|weighted_contributions|)` uses the FULL composite (alignment is about feature agreement, not direction). `regime_fit` is `1.0` when `regime_allows()` returned True else `0.0`; `no_conflicts` is `1.0` when no gates have already failed else `0.5`; `data_freshness = min(1.0, len(df)/200)`. The IV-RV term the spec lists under conviction inputs is deferred to Phase 2.5 (microstructure) when the Deribit IV feed lands. The per-asset-class threshold table `DUAL_SCORE_THRESHOLDS` (BTC/ETH `0.58/0.60`, MID_CAP_DEFAULT `0.62/0.68`, FOREX `0.55/0.55`, METAL `0.57/0.60`, STOCK `0.56/0.58`) lives in the same module with `dual_score_thresholds_for(asset_class)` defaulting to the BTC/ETH bar for unknown classes (fail-closed). `quant/main.py` calls `compute_dual_score` AFTER the regime gate (so `regime_fit` is populated) and BEFORE the R:R / noise gates (so `alignment` doesn't double-count); when either bound fails it appends `below_thresholds` to `gates_failed`. The legacy single-knob `z_threshold` gate (`|composite| < Z_THRESHOLD=1.5`) is REMOVED from `compute_composite` — running both produced false rejections in the |z|≈1.06–1.50 band where dual-score passed but the legacy 1.5 floor still blocked, surfacing as `below_thresholds` on signals the new spec considers OK. With `z_threshold` gone, `below_thresholds` now has ONE source (the dual-score gate) and `gates_failed` cannot contain both names. The priority list in `main.py` was simplified accordingly (no more legacy → spec name remap). `quant/models.py` `ScoreResponse` extended with optional `direction_probability` and `conviction` (both NaN-sanitized through `_f`). `server/quantClient.ts` `QuantScoreResponse` interface mirrors the new fields. `server/prompts/signalGen.ts` extends the `quantPrepass` input type with `direction_probability` + `conviction` and emits them in the `SCORER PREPASS` line at 4-decimal fixed precision (snapshot-stable). `server/prompts/shared.ts` SCORER-AUTHORITATIVE block now also instructs the AI to emit the transmitted scorer values into `TradePlanSchema.direction_probability` / `.conviction` (the 4-decimal value as printed is the source of truth — no substituting the AI's own estimate) and to defer to `scorer_no_signal_reason="below_thresholds"` rather than talking the trade up by raising its own conviction estimate. `server/routes.ts` V2 shadow IIFE extends the inline `quantPrepass` type and forwards both fields when the scorer returned them. End-to-end smoke confirmed live across all 4 asset classes (post-architect-fix run): BTC `dir_prob=0.5391 / conviction=0.7135` (gated by regime_mismatch + below_thresholds), ETH `0.5447/0.7296`, XAU `0.6389/0.8000`, EURUSD `0.5905/0.7394`, NVDA `0.5556/0.7592`. Verified: (1) `z_threshold` no longer in `gates_failed` for any class, (2) `below_thresholds` appears at most once, (3) BTC scorer's `dir_prob=0.5391` matches the expected directional-only formula output `0.5391` exactly (carry+volgate excluded confirmed), (4) PREPASS line carries 4-decimal values, (5) legacy callers without `quantPrepass` produce no PREPASS line (backward compat). **Phase 2.3 — Vol-Percentile-Adjusted R:R migrated to Python (DONE):** `quant/scorer.py` adds `compute_vol_adjusted_rr(df, regime)` returning `(vol_percentile, rr_multiplier)` from a 90-bar percentile rank of `ATR(14) / close` (ratio used instead of raw ATR so a 60k→70k drift in BTC doesn't inflate the bucket — documented in code comments). Buckets are spec-literal `0.20 / 0.60 / 0.85 → 0.70 / 1.00 / 1.30 / 1.60`, with the RANGE regime cap at 1.00 (mean-reversion targets shouldn't extend). Returns the `(0.50, 1.00)` baseline under degenerate input. **Phase 2.4 — Kelly meta-shrinkage (DONE, with PROXY meta-label):** `compute_p_loss_meta_proxy(direction_probability) = clip(1 - direction_probability, 0, 1)` is documented as a Phase-3 placeholder until a trained gradient-boosted meta-classifier on labelled `(entry, SL, TP1)` triples lands; rationale on the function: `direction_probability` already encodes `P(reach TP1 before SL)`, so its complement is the natural placeholder. `kelly_fraction_applied` is computed server-side in `server/routes.ts` (not in the scorer — kelly_base needs per-(token, direction) calibration only the route knows): `min(0.25, kelly_base, kelly_base * (1 - p_loss_meta) * regime_size_modifier * conviction)` with `regime_size_modifier = 0.5` in `HIGH_VOL` else `1.0`. Double-cap (0.25 AND kelly_base) enforces "never size up because p_loss_meta is low". **Phase 2.5 — Crypto microstructure (CVD + OBI; IV-RV deferred) (DONE):** `quant/scorer.py` adds `compute_microstructure(coin, side, df, base_conviction, asset_class)`. Crypto-only (BTC / ETH / SOL / MID_CAP_DEFAULT); STOCK / COMMODITY / FX return an all-`null` block. **§5a CVD:** uses `cvd_z` z-score (≥30 samples) plus the 20-bar return correlation to detect bullish/bearish divergence (`+0.06` / `−0.06` to `direction_probability`) and confirmation (`+0.04` / `−0.04`); contradiction at `base_conviction < 0.65` raises the `cvd_contradict` extra gate (the spec's hard shutoff at low conviction). **§5b OBI:** parses the top-10 levels of the live HL `l2Book`, applies the spec ±0.20 thresholds with asymmetric `+0.05` / `−0.10` conviction deltas (bigger penalty for fighting the book than reward for going with it). Staleness is gated by a NEW `STATE.books_ts` map stamped per-book in `quant/hl_ws.py` the moment a fresh `l2Book` frame is written — book-ts (NOT cvd-tip-ts) is the freshness source so a coin with fresh trades but a frozen book correctly degrades to null OBI. **§5c IV-RV:** always emits `null` (Deribit ATM 7-day IV feed pending procurement). All §5 adjustments are folded into `direction_probability` and `conviction` BEFORE the threshold gating in `quant/main.py`, and the SCORER-AUTHORITATIVE blocks in `server/prompts/shared.ts` §5 explicitly tell Claude not to re-apply the same deltas. **Prompt extensions (DONE):** `server/prompts/shared.ts` SIGNAL_ENGINE_V1 §3 / §4 / §5 each open with a SCORER-AUTHORITATIVE block instructing Claude to USE the exact 4-decimal values from the SCORER PREPASS line (vol_percentile, rr_multiplier, p_loss_meta, kelly_fraction_applied, microstructure object) and emit them verbatim into TradePlanSchema; §5 explicitly preserves the `cvd_contradict` NO_SIGNAL passthrough. Each block has a "FALLBACK ONLY" section describing the in-prompt math used when the scorer didn't transmit those fields (backward-compat with no-prepass callers). `server/prompts/signalGen.ts` extends the `quantPrepass` input type with the new fields and emits them in the PREPASS line at 4-decimal stable formatting with per-field omit when undefined. `server/quantClient.ts` `QuantScoreResponse` mirrors the new fields. `server/routes.ts` V2 shadow IIFE extends the `quantPrepass` type, computes `kelly_fraction_applied` server-side using imported `computeKellyFraction`, and forwards everything. **Live smoke confirmed:** BTC scoring returned `regime=TREND_UP, vol_percentile=0.82, rr_multiplier=1.30 (correct bucket for 0.60≤vp<0.85), p_loss_meta=0.466, microstructure.obi=0.18 (book is fresh — proves new per-book staleness path works)`. Stock (AAPL) correctly returns `microstructure: cvd_state="n/a", obi=null, ivrv_spread=null` while still emitting vol_percentile / rr_multiplier. **Phase 2 microstructure data-feed gap remaining:** Deribit ATM 7-day IV feed for BTC/ETH (IV-RV §5c — currently always null in the response). **Morning-brief no-send fix (DONE — debug of Apr 30 2026 6 ET miss):** `server/dailyBrief.ts` rewrites `_attemptBrief()` to inspect the structured `BriefSendResult` rather than relying on "did it throw" — `enqueueDailyBrief()` returns `ran:true / sent:0` (without throwing) for genuine failures (pre-send exception, data integrity abort) AND those paths release the DB slot, so the previous "stamp-then-await" pattern was permanently blocking same-process retries on a non-throwing failure. New stamp matrix: `res === null` (queued via BullMQ) → stamp; `res.ran === false` (claimed by another process) → stamp; `res.sent > 0` → stamp; `"no active subscribers"` → stamp (no point retrying same day); anything else (Pre-send exception, Data integrity gate with sent=0) → leave UNSTAMPED for the next 15-min heartbeat tick / 6 ET fire to retry. Adjacent supports already in place: `_briefInFlight` guard prevents the heartbeat from racing the 6 ET tick, and the boot catch-up tick (hour ≥ BRIEF_HOUR_ET AND DB slot unclaimed → fire late) keeps a Railway restart after 6 ET from skipping the day. **Forbidden-file constraint reaffirmed for Phase 2.3-2.5:** No `shared/schema.ts` (Drizzle) changes, no DB schema changes, no `db:push` run, no `ALTER TABLE` — same as Phase 2.1 / 2.2.
- **Access Code Redemption (Race-Safe, Per-User Ledger):** `/api/verify-code` is hardened against the live spam-redemption exploit on shared codes (e.g. `CLVR-VIP-GROUP2026`). Each access code now carries a `redemption_type`:
  - `single_use_global` — single-use codes that lock to the first redeemer (default for individual VIPs, trial codes, admin Pro codes).
  - `single_use_per_user` — group/shared codes (CLVR-VIP-GROUP2026) that any verified user may redeem **exactly once each**.
  Enforcement combines (a) an email-verified gate, (b) a 5-attempt-per-hour per-user audit-backed rate limit, (c) a transactional `SELECT … FOR UPDATE` on the access-code row plus `INSERT … ON CONFLICT DO NOTHING` against the `code_redemptions` ledger (UNIQUE(code, user_id)), (d) a tier-no-downgrade rule, and (e) a no-shorten expiry rule that never reduces a user's existing access window — `promo_code` / `promo_expires_at` are only refreshed when the new code STRICTLY EXTENDS the user's current access (a NULL/permanent expiry always wins over any finite date). Every attempt — success, duplicate, expired, exhausted, rate-limited, error — writes a row to `redemption_attempts` for forensics. A one-time grandfather backfill seeds prior `users.promo_code` claims into the ledger so legitimate holders are never charged twice. Both new tables and the `redemption_type` column were added via additive raw SQL `CREATE TABLE IF NOT EXISTS` / `ADD COLUMN IF NOT EXISTS` in `server/initDb.ts` (no Drizzle schema changes, no `db:push` run).
  - **Documented exception to the "Drizzle only" preference:** the redemption transaction in `server/routes.ts` uses raw `pg` (`pool.connect()` → `BEGIN` / `SELECT FOR UPDATE` / `INSERT ON CONFLICT` / `COMMIT`) because Drizzle's high-level API does not cleanly express row-locked inserts with conflict-aware short-circuiting in a single transaction. The handler always releases the client in `finally` and writes a `claim_error` audit row on any catch.

## External Dependencies
- **Anthropic Claude:** For AI-driven insights and signal vetoing.
- **PostgreSQL:** Primary database.
- **Drizzle ORM:** For PostgreSQL interaction.
- **Python FastAPI:** Powers the quantitative microservice.
- **Hyperliquid WebSocket:** Real-time crypto perp price feeds.
- **Stripe:** Subscription management.
- **Resend:** Email sending service.
- **GitHub:** Development workflows.
- **CryptoPanic:** News aggregation.
- **Yahoo Finance API:** Market data for equities, ETFs, metals, forex, and indices.
- **FMP API (Financial Modeling Prep):** Fallback market data.
- **Binance WebSocket:** Browser-direct crypto spot ticker updates.
- **CCXT:** Python library for cryptocurrency exchange integration within the quant service.
- **ExchangeRate-API:** Forex fallback.
- **gold-api.com:** Metals fallback.