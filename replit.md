# CLVRQuantAI

## Overview
CLVRQuantAI is a luxury, mobile-first market intelligence dashboard for real-time analysis across crypto, equities, commodities, and forex. It provides AI-driven market insights, a macro calendar, personalized alerts, and a morning brief to support informed trading decisions. The project aims to transition from AI-originated signals to a deterministic quant scorer, with AI serving a veto-only role, enhancing signal reliability and precision.

## Recent Decisions
- **336-bar 1h lookback (14d × 24h)** is the canonical archetype-classifier window everywhere — `/api/quant`, `/api/ai/analyze`, `/api/kronos`. Anything shorter silently collapses the daily-ATR aggregate.
- **Hourly cron + materialized views** (`archetype_stats`, `archetype_scorecard`) feed all per-archetype stats; the TS-direct fallback in `getArchetypeStats` only fires when the MV is empty or unreachable (cycle-safe).
- **Funding + OI snapshots are plumbed once** via `microstructureSnapshot.ts` and consumed identically across endpoints — classifier MEAN_REV clauses evaluate independently so a NULL funding field never poisons OI/Bollinger evaluation.
- **Support owner alerts (both channels, once-per-episode email):** a client reach-out emails the owner at **Support@clvrquantai.com** exactly ONCE per conversation episode (best-effort, never blocks the send) AND surfaces an owner-only in-app pill + toast driven by `GET /api/support/unread-count` (15s poll), which deep-links to Account → Support. Dedup is enforced by the additive `support_threads.owner_notified_at` column: both `/escalate` and `/api/support/message` atomically CLAIM the slot via `UPDATE … SET owner_notified_at=NOW() WHERE id=$1 AND owner_notified_at IS NULL RETURNING id` before sending, and RELEASE it back to NULL if the send throws (so a transient Resend failure retries next message instead of permanently suppressing). Closing a chat (either side) resets `owner_notified_at=NULL`, so a fresh reach-out re-notifies. The owner email constant (`SUPPORT_OWNER_EMAIL`) also governs `/escalate` (to) and `/reply` (replyTo).
- **Support live-chat UX (both parties):** `support_threads` carries additive `user_typing_at` + `owner_typing_at` columns. New heartbeat endpoints `POST /api/support/typing` (user) and `POST /api/support/thread/:id/typing` (owner) are pinged on textarea input (throttled ~1/2.5s); the read endpoints derive a typing boolean with a 6s freshness window, and sending a message/reply clears the sender's flag. Client poll cadence is 3s while a human chat is open. Ending a chat (user `/close` or owner `/thread/:id/close`) inserts a visible system "ended" message so BOTH parties see it; the owner close is guarded to skip already-closed threads (no duplicate marker). The user thread poll (`GET /api/support/thread`) returns the LATEST thread incl. closed (with `{closed, ownerTyping}`) so the Concierge widget shows an "ended" banner + "Start a new chat" instead of the chat vanishing. The owner SupportInbox root has `paddingRight` so the floating Concierge "C" button no longer overlaps the conversation.

## User Preferences
- I prefer simple language.
- I like functional programming.
- I want iterative development.
- Ask before making major changes.
- I prefer detailed explanations.
- Do not make changes to `package.json`, `vite.config.ts`, `server/vite.ts`, `drizzle.config.ts`.
- Do not use raw `pg` on Node – Drizzle only.
- `CLAUDE_MODEL` stays `claude-sonnet-4-6`.
- Use `packager_install_tool` for any package install (Python or Node).

## System Architecture
The application features an Express (TypeScript) backend and a React (JSX) frontend, served via Vite on a single port. Data is persisted in PostgreSQL using Drizzle ORM. A Python FastAPI quant microservice handles deterministic quantitative scoring. The UI/UX employs a navy and gold theme with Playfair Display and IBM Plex Mono fonts.

**Key Architectural Components & Features:**
- **Quant Microservice:** A Python FastAPI service (`quant/`) generates deterministic quantitative scores, including modules for scoring, risk management (stop-loss, sizing), regime detection, GARCH modeling, and microstructure analysis.
- **Node-Python Bridge:** `server/quantClient.ts` facilitates communication between the Node.js backend and the Python quant microservice.
- **Real-time Data Aggregation:** Aggregates 1-minute OHLCV bars from sources like Hyperliquid, Binance, and Yahoo Finance.
- **Signal Generation & Hardening:** Signals originate from the quant service, are subject to cost/EV gates, and an AI veto. Signal hardening rules (e.g., `MIN_RR_AFTER_FRICTION`) optimize throughput and reduce noise.
- **Statistical Brain:** An empirical edge engine (`server/lib/statisticalBrain.ts`) analyzes historical trade data to compute win rates, expected returns, and resolution times per (token, direction) combo. It derives strict limits for Take Profits, Stop Losses, and Kill Clocks, issuing verdicts: **INVERT** (WR<15% & n≥20, opposite side not also weak — flip direction), SUPPRESS (WR<25% & n≥20), CAUTION, NORMAL, or PREFERRED. This brain context is integrated into the AI's prompts and can veto/flip signals.
- **Edge Policy (Phase 0 — INVERT loser combos):** `applyEdgePolicy(token, direction, rawConvictionPct)` and `recalibrateConviction()` in `server/lib/statisticalBrain.ts` translate brain verdicts into deterministic post-LLM actions: SUPPRESS drops the card; INVERT mirrors stop/targets around entry via `mirrorPrice()` and flips direction; conviction recalibration clamps INVERT/SUPPRESS verdicts at ≤25 and caps any raw≥50 at 40 for NORMAL/CAUTION, while PREFERRED passes through untouched. Wired into BOTH `/api/ai/analyze` `hardenTradeIdeas` and `/api/kronos` BEFORE `hardenSignal()`. Cards expose `edgePolicy: {action, originalDirection, recommendedDirection, brainVerdict, rawConvictionPct, recalibratedConvictionPct, reason}` and an `edge-inverted` hardener flag; INVERT forces `materiallyMutated=true` to trigger rationale regen. Falls open on any error.
- **Chart Vision:** `server/lib/chartRenderer.ts` generates SVG candlestick charts with technical overlays (EMAs, S/R, entry zones), converts them to PNG, and sends them as multimodal input to Claude for visual analysis validation.
- **Enhanced AI Analysis (`/api/ai/analyze`):** Integrates the Statistical Brain, Chart Vision, and intraday Execution Levels (VWAP, ORH, ORL) into the AI's context for a comprehensive analysis of user-specified tickers.
- **Daily Brief Integration:** The Statistical Brain context is used to bias trade idea selection in the daily email brief and Telegram posts, favoring empirically winning setups and avoiding suppressed ones.
- **Trade Idea Asset Class Selector:** UI allows filtering trade ideas by asset class (All, Crypto, Equities, Commodities, Forex), dynamically adjusting market types (PERP/SPOT) and filtering signals accordingly.
- **Telegram Autoposter:** A unified helper (`server/lib/buildEnrichedReasoning.ts`) creates consistent, branded captions for live signals and daily brief trade ideas, posted via a dedicated webhook. Includes admin tools for testing posts.
- **Email & Update Management:** Admin tools for email diagnostics and an `UpdateLogManager` for curating and AI-generating weekly project updates. Promo expiry reminders are managed with idempotency to prevent duplicate sends.
- **Improvement Log Mirroring:** Agent's `logImprovement()` mirrors entries to a production database for centralized tracking via HMAC-SHA256 signed HTTP.
- **Session Overlays:** Per-session VWAP (with ±1σ bands) and Opening Range (ORH/ORL) overlays are displayed on live charts for spot equities, FX, and commodities, and provided to the AI Analyst.
- **Signal Engine v1 (deterministic Python scorer feeds prompt; AI is veto-only):** Spec encoded as `SIGNAL_ENGINE_V1` constant in `server/prompts/shared.ts`, injected into v2 signal-gen prompt at `server/prompts/signalGen.ts`. `TradePlanSchema` extended with optional fields (`signal_status`, `regime`, `direction_probability`, `conviction`, `p_loss_meta`, `vol_percentile`, `rr_multiplier`, `kelly_fraction_applied`, `microstructure`, `gates_passed`, `no_signal_reason`) — all optional for backward compat. New `NO_TRADE_REASONS`: `regime_chop`, `regime_mismatch`, `below_thresholds`, `cvd_contradict`, `ivrv_block`. Implementation phases (all DONE except IV-RV):
  - **2.1 Regime Gate (Python):** `quant/regime.py` — 5-state classifier using `percentile_rank(ATR(14),90) + ADX(14) + BB_width + EMA alignment`. `regime_allows(regime, side, signal_type)` enforces allowed-types matrix. SCORER-AUTHORITATIVE prepass injected into AI prompt.
  - **2.2 Dual Score (Python):** `compute_dual_score()` returns purely-directional `direction_probability` and `conviction`. Per-asset-class thresholds in `DUAL_SCORE_THRESHOLDS`; fail-closed default. Legacy `z_threshold` gate REMOVED.
  - **2.3 Vol-Adjusted R:R (Python):** `compute_vol_adjusted_rr()` returns `(vol_percentile, rr_multiplier)` from 90-bar percentile rank of `ATR(14)/close` ratio. RANGE regime capped at 1.00.
  - **2.4 Kelly meta-shrinkage:** `compute_p_loss_meta_proxy = clip(1 - direction_probability, 0, 1)`. `kelly_fraction_applied = min(0.25, kelly_base, kelly_base*(1-p_loss_meta)*regime_size_modifier*conviction)` with `HIGH_VOL → 0.5x` modifier.
  - **2.5 Crypto microstructure (CVD+OBI live; IV-RV deferred):** `compute_microstructure()` crypto-only. CVD ±0.06/0.04 deltas + hard `cvd_contradict` gate at base_conviction<0.65. OBI top-10 levels with asymmetric +0.05/−0.10 deltas. All §5 adjustments fold into dir_prob/conviction BEFORE threshold gating.
- **Trade Ideas Hardener (post-LLM, /api/ai/analyze + /api/kronos):** Mechanical risk gates run AFTER the LLM — `server/lib/signalHardening.ts`'s `hardenSignal()`: ATR(1h)-floored stop, CHASE flag (>4% adverse 24h move), CROWDED flag (funding ≥0.01%/8h + OI rising), Wilson 95% CI on backtest WR, quarter-Kelly size off CI lower bound, regime leverage cap, post-haircut conviction veto. Materially-mutated cards trigger an optional prose regen via `server/lib/rationalePrompt.ts`. Wrapped in try/catch — on any hardener error the original LLM output flows through unmodified.
- **Morning-brief no-send fix (Apr 30 2026 miss):** `server/dailyBrief.ts` `_attemptBrief()` inspects structured `BriefSendResult` instead of "did it throw". Stamp matrix: queued/claimed/sent>0/no-subscribers → stamp; pre-send exception or data-integrity sent=0 → leave UNSTAMPED for retry. `_briefInFlight` guard prevents heartbeat racing 6 ET tick; boot catch-up tick fires late if Railway restarted past BRIEF_HOUR_ET.
- **Access Code Redemption (Race-Safe, Per-User Ledger):** `/api/verify-code` is hardened against the live spam-redemption exploit on shared codes (e.g. `CLVR-VIP-GROUP2026`). Each access code carries a `redemption_type`:
  - `single_use_global` — locks to the first redeemer (default for individual VIPs, trial codes, admin Pro codes).
  - `single_use_per_user` — group/shared codes any verified user may redeem exactly once each.
  Enforcement combines (a) email-verified gate, (b) 5-attempt-per-hour per-user audit-backed rate limit, (c) transactional `SELECT … FOR UPDATE` + `INSERT … ON CONFLICT DO NOTHING` against the `code_redemptions` ledger, (d) tier-no-downgrade rule, (e) no-shorten expiry rule. Every attempt writes a `redemption_attempts` row. One-time grandfather backfill seeds prior `users.promo_code` claims into the ledger. Tables + `redemption_type` added via additive raw SQL in `server/initDb.ts`.
  - **Documented exception to the "Drizzle only" preference:** the redemption transaction in `server/routes.ts` uses raw `pg` because Drizzle does not cleanly express row-locked inserts with conflict-aware short-circuiting in a single transaction.

- **Module 3 — PostTradeAnalyzer (Phase A, May 2026):** Every resolved scanner signal gets a one-line post-trade diagnosis (+0–3 secondary tags) via a delayed worker, surfaced in a user-facing Journal tab and a Sunday weekly model report. Phase A ships tagger + journal UI + report **in shadow mode**; Phase B (auto-adjust feedback loop, 90d bootstrap) is explicitly deferred. Four flags: `PTA_ANALYZER_ENABLED` (default true — gates the worker), `PTA_JOURNAL_DISPLAY_ENABLED` (default false — gates the journal chips), `PTA_WEEKLY_REPORT_ENABLED` (default false — gates the Sunday send), `PTA_MODEL_ADJUSTMENTS_ENABLED` (Phase B, read-only this session).
  - **Schema (additive in `server/initDb.ts`):** `post_trade_analysis` (UNIQUE signal_id, primary_tag, secondary_tags TEXT[], assigned/actual archetype, mfe_r, mae_r, diagnosis_confidence, explanation_text, diagnostics JSONB, 4 indexes); `model_adjustments` (Phase B writes only); `archetype_scorecard` materialized view grouped by `(archetype, vol_regime)` with trailing-50 effective WR + top-3 diagnosis tags; `stats_refresh_log.mv_name` column added so refresher rows are tagged per MV.
  - **Analyzer (`server/lib/postTradeAnalyzer.ts`):** Pure module exporting `collectAnalysisInputs`, `diagnose` (13-rule first-match-wins engine — rules with null required inputs skip silently), `secondaryChecks` (3 independent meta tags), `explainTag` (deterministic templates — no AI call in Phase A). Tolerates missing fields; current `exit_reason` vocab is `TP*_HIT|SL_HIT|EXPIRED_*` so rules keyed on `thesis_invalidated` / `stale_flat` simply don't fire today.
  - **Worker (`server/lib/postTradeAnalyzerWorker.ts`):** 60s single-flight tick. Picks ≤100 rows from `post_trade_analysis WHERE primary_tag='PENDING_ANALYSIS' AND signal.resolved_at < NOW() - 35min` so post-exit windows have time to materialize. Per-row fail-open (leaves PENDING for retry).
  - **Resolver hook:** `server/lib/outcomeResolver.ts` calls `enqueuePostTradeAnalysis(id)` only when `.returning({id})` confirms it won the PENDING→resolved compare-and-set race — guarantees single-fire per signal. Wired only at the 2 REAL update sites; shadow-inversion sites are intentionally NOT hooked.
  - **MV refresher extension:** `server/lib/statsMvRefresher.ts` now refreshes BOTH `archetype_stats` and `archetype_scorecard` on the same hourly cadence via an internal `refreshOneMV()` helper. Independent try/catch per MV; each writes its own `stats_refresh_log` row tagged with `mv_name`.
  - **Journal UI:** `client/src/App.jsx`'s existing JOURNAL tab now renders a new `SignalsDiagnosisPanel` above the manual trade log. Panel calls `GET /api/journal`, shows a chip-per-row table with color mapping (green=clean win, red=clean loss, amber=meta/neutral, grey=PENDING/UNCLASSIFIED) and the deterministic one-sentence explanation in a tooltip. Short-circuits to a "coming soon" card when both `VITE_PTA_JOURNAL_DISPLAY_ENABLED!=='1'` AND server reports flag off, so the nav entry never 404s.
  - **Weekly report (`server/lib/weeklyModelReport.ts`):** `buildReport()` joins `archetype_scorecard` + `model_adjustments` + 7d-vs-prior-7d net WR delta. Pure-data report object feeds both `formatHtmlEmail` + `formatMarkdown`. Scheduler fires Sunday 22:00 America/New_York (1-min tick + date-key dedupe). Email send is best-effort and only attempts when `RESEND_API_KEY` + `WEEKLY_MODEL_REPORT_RECIPIENTS` are set.
  - **Admin endpoints:** `GET /api/journal` (auth), `GET /api/reports/weekly-model` (Elite), `POST /api/admin/pta/generate-report` (admin one-shot), `POST /api/admin/pta/run-worker-once` (admin one-shot).
  - **Constraints honored:** no `shared/schema.ts` edits, no `db:push`, no raw `pg` (M2 redemption exception still stands), no edits to forbidden config files.

- **Module 2 — Setup Taxonomy + Per-Setup Stats (May 2026, T01-T14 shipped):** Full Module 2 rollout extending Module 1 archetypes into a shared classifier, true daily ATR via Wilder smoothing, cross-endpoint funding/OI parity, Wilson 80% LCB, materialized-view stats with hourly cron, and four feature flags for safe phased rollout. **(T01)** `server/lib/archetypeClassifier.ts` wraps `classifyArchetype` with `ARCHETYPE_LOOKBACK_1H = 336` (14d×24h), `dataResolution`-aware input validation, and per-call `classification_diagnostics`; `buildArchetypeContext` uses Wilder ATR (`ATR_t = (ATR_{t-1}*13 + TR_t)/14`) once ≥15 daily bars exist, falling back to simple mean below that. **(T02)** `server/lib/microstructureSnapshot.ts` exports a shared `getMicrostructureSnapshot(token)` reading funding (30min staleness) + OI (5min staleness) from the HL cache. /analyze + /kronos call it for crypto perps with `audit: "available"|"unavailable"|"no_concept"`; classifier's MEAN_REV clauses (a/b/c) evaluate independently so NULL funding doesn't poison OI/Bollinger evaluation. **(T03)** All three endpoints (/api/quant, /api/ai/analyze hardenTradeIdeas, /api/kronos) classify through the same `classifyForLive(ctx)`; /quant retains its MEAN_REV auto-flip (mirror SL/TP1-TP3 around entry BEFORE brain check), /analyze + /kronos classify AFTER edge policy and expose `recommendedFlip` boolean without auto-flipping. **(T04)** `getArchetypeStats` returns both `wrPointEst` and `wrWilsonLB80` (z=0.8416); SignalCard shows "Win Rate: X% (n=N)" with LCB80 primary, "(n=N, raw Y%) — LOW SAMPLE" amber prefix when n≤20, falls back to `wr_wilson_lb` 95% if server is older. **(T05)** `ARCHETYPE_SUPPRESSION_ENABLED` (default false). In shadow mode UNCLASSIFIED publishes AND writes a `suppressed_signals` row with reason `would_suppress_no_archetype`; treated as a 7th archetype in `getArchetypeStats`. **(T06)** `server/scripts/backfillArchetypes.ts` (admin-triggered via `POST /api/admin/archetype/backfill`): crypto-only Binance 1h historical via endTime pagination, 336-bar windows ending at each signal's `created_at`, allowed archetypes restricted to TREND_PULLBACK / RANGE_FADE / MEAN_REVERSION_EXHAUSTION (others short-circuit to None on historical data). Idempotent by `(source_signal_id, classifier_version)`. **(T07)** `USE_BACKFILLED_STATS` flag UNIONs live `ai_signal_log` with `backfilled_classifications`. **(T08)** `wilson_lcb(wins, total, confidence)` plpgsql IMMUTABLE function + `archetype_stats` materialized view grouped by `(archetype, classification_source)` with `PERCENTILE_CONT` for median/p75, `UNIQUE INDEX archetype_stats_pk` for CONCURRENTLY refresh. **(T09)** `server/lib/statsMvRefresher.ts` (5-min heartbeat, refreshes at :05 past every hour + :35 during 13:30-21:00 UTC US cash hours, 15min debounce floor, REFRESH CONCURRENTLY with non-concurrent fallback). `POST /api/admin/archetype/refresh-stats` for manual triggers. **(T10)** `server/lib/statsRepository.ts` exports `IStatsRepository` with `TsStatsRepository` (5min cache) and `MaterializedViewStatsRepository`. `STATS_SOURCE = "ts" | "mv"` (default "ts"); `getArchetypeStats` routes to MV first, falling open to TS on empty/error. `startShadowCompareScheduler` (30min) writes any divergence >1pp on Wilson 80% LCB to `stats_divergence_log`. **(T11)** `USE_ARCHETYPE_DISPLAY` flag (default false). Archetype badge abbreviations: NEWS_MOMO→NEWS, MEAN_REVERSION_EXHAUSTION→MEAN-REV, BREAKOUT_RETEST→BREAKOUT-RT, VWAP_RECLAIM→VWAP-RCLM, TREND_PULLBACK→PULLBACK, RANGE_FADE→RANGE. **(T12)** New admin tab "ARCH" (`client/src/components/admin/ArchetypeAdminPanel.jsx`) — Coverage by Asset Class, Per-Endpoint Distribution, WR per Archetype (live | backfill | combined) with Δ-from-global flag, Top 5 UNCLASSIFIED Near-Miss Reasons, Recent MV Refresh Log. Single endpoint `GET /api/admin/archetype/summary`. **(T13)** `server/tests/archetypeFixture.test.ts` — 12 hand-curated snapshots, 12/12 passing. **All Module 2 DB infra is additive** — `ai_signal_log` columns `classification_source TEXT` + `classification_diagnostics JSONB`, plus 4 new tables (`suppressed_signals`, `backfilled_classifications`, `stats_divergence_log`, `stats_refresh_log`), 1 plpgsql function, 1 materialized view + unique index. No `shared/schema.ts` edits, no `db:push`, no raw `pg` (redemption exception stands).

- **Module 2 Phase 0 — Taxonomy Foundation:** Shipped (superseded by full Module 2 entry above; see git history for original 3-task slice — 336-bar lookback, Wilder ATR, and 6 additive DB objects).
- **Module 1 — Setup Archetypes:** Shipped (superseded by full Module 2 entry above; see git history for the original `archetype.ts` 6-archetype classifier and the `/api/quant` MEAN_REV auto-flip wiring).

## External Dependencies
- **Anthropic Claude:** For AI-driven insights and signal vetoing.
- **PostgreSQL:** Primary database.
- **Drizzle ORM:** For PostgreSQL interaction.
- **Python FastAPI:** Powers the quantitative microservice.
- **Hyperliquid WebSocket:** Real-time crypto perp price feeds.
- **Stripe:** Subscription management.
- **Resend:** Email sending service.
- **GitHub:** Development workflows.
- **CryptoPanic:** News aggregation.
- **Yahoo Finance API:** Market data for equities, ETFs, metals, forex, and indices.
- **FMP API (Financial Modeling Prep):** Fallback market data.
- **Binance WebSocket:** Browser-direct crypto spot ticker updates.
- **CCXT:** Python library for cryptocurrency exchange integration within the quant service.
- **ExchangeRate-API:** Forex fallback.
- **gold-api.com:** Metals fallback.
