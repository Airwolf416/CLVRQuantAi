TASK: Add a multi-stage Dockerfile at the repo root that builds the app on GitHub Actions (16GB RAM) instead of Railway's builder. This is piece 1 of a 3-part migration to prebuilt images. Do NOT change nixpacks.toml, the workflow, or Railway settings yet — those are later steps. Just create the Dockerfile and verify it builds.

CONTEXT (verified against the repo)
- rest-express@1.0.0, Node >=22.12. Build = `tsx script/build.ts` -> emits dist/index.cjs (esbuild, CJS) AND dist/public/ (vite client).
- server/static.ts serves the client from path.resolve(__dirname, "public"), i.e. dist/public MUST sit next to dist/index.cjs at runtime.
- start = `NODE_ENV=production node dist/index.cjs`, launched via PATH=/opt/venv/bin:$PATH so the app can shell out to the Python quant scripts in ./quant.
- There is a Python quant layer: a venv at /opt/venv with deps from quant/requirements.txt. This MUST exist in the final image and be on PATH.

STEP 1 — Confirm the Python deps file before writing anything (read-only)
- Print quant/requirements.txt. If it does NOT exist, search the repo for how Python deps are declared (pyproject.toml / uv.lock) and report what you find. Do not guess — if it's not requirements.txt, STOP and tell me; I'll adjust the Dockerfile.

STEP 2 — Create ./Dockerfile with EXACTLY this content:

# ---------- Stage 1: build (Node) ----------
FROM node:22-bookworm AS build
WORKDIR /app

# Install ALL deps (incl. dev) using the lockfile. This runs on GitHub
# Actions (~16GB RAM), so the npm ci that OOM-killed Railway's builder
# has plenty of memory here.
COPY package.json package-lock.json ./
RUN npm ci --include=dev --legacy-peer-deps --no-audit --no-fund

# Build client + server.
COPY . .
RUN NODE_OPTIONS=--max-old-space-size=4096 npm run build

# Drop dev dependencies so we copy only production node_modules forward.
RUN npm prune --omit=dev --legacy-peer-deps

# ---------- Stage 2: runtime ----------
FROM node:22-bookworm-slim AS runtime
WORKDIR /app

ENV NODE_ENV=production
ENV PYTHONUNBUFFERED=1

# Python for the quant layer (venv lives at /opt/venv, matching the old start cmd).
RUN apt-get update \
 && apt-get install -y --no-install-recommends python3 python3-venv python3-pip \
 && rm -rf /var/lib/apt/lists/*

# Build the venv and install quant deps.
COPY quant/requirements.txt ./quant/requirements.txt
RUN python3 -m venv /opt/venv \
 && . /opt/venv/bin/activate \
 && pip install --upgrade pip \
 && pip install -r quant/requirements.txt

# Pruned production node_modules + compiled app from the build stage.
COPY --from=build /app/node_modules ./node_modules
COPY --from=build /app/dist ./dist

# App source needed at runtime (quant scripts, package.json, anything read at runtime).
COPY . .

# Put the venv on PATH so the server can invoke python the same way it did
# under the old `PATH=/opt/venv/bin:$PATH npm start`.
ENV PATH="/opt/venv/bin:$PATH"

CMD ["node", "dist/index.cjs"]

STEP 3 — Add a .dockerignore at repo root (create or replace) containing exactly:
node_modules
dist
.git
.github
attached_assets
exports
*.log

(Keeps the build context small/fast. node_modules and dist are rebuilt inside the image, so they must not be copied from your working tree.)

STEP 4 — Verify locally in the Replit shell (do NOT deploy):
- Run: docker build -t clvrquant-test .
  If Docker isn't available in this environment, say so and skip — we'll let GitHub Actions do the real build in piece 2. Do not treat that as failure.
- If it builds, confirm the image has BOTH /app/dist/index.cjs and /app/dist/public/index.html, and that /opt/venv/bin/python exists.

GUARDRAILS
- Do NOT modify nixpacks.toml, package.json, tier gating, signal/quant code, or any Railway setting in this step.
- Do NOT add a GitHub Actions workflow yet (that's piece 2).
- Do NOT remove googleapis or change deps — we're now solving this at the build layer, so the heavy tree is fine; GitHub Actions has the memory for it.
- If quant/requirements.txt is missing or Python deps come from somewhere else, STOP after Step 1 and report.

ACCEPTANCE
- ./Dockerfile and ./.dockerignore exist with the content above.
- If Docker was available: image builds; dist/index.cjs, dist/public/index.html, and /opt/venv/bin/python all present.
- nixpacks.toml and all app code unchanged.

COMMIT
- Message: "build: add multi-stage Dockerfile for prebuilt-image deploys (piece 1/3)"
- Push to main.

NOTE: Pushing this alone will NOT change your Railway deploy yet — Railway is still on the nixpacks/GitHub-repo source until piece 3. So this commit is safe and won't trigger the broken build path differently. Report back what Step 1 printed and whether Docker was available.
