          {/* Regime Gate badge */}
          {result.regime_gate && (
            <div style={{
              background: result.regime_gate.action === "BLOCK" ? "rgba(255,45,85,0.08)"
                        : result.regime_gate.action === "DOWNGRADE" ? "rgba(245,158,11,0.08)"
                        : "rgba(0,255,136,0.06)",
              border: `1px solid ${result.regime_gate.action === "BLOCK" ? "rgba(255,45,85,0.35)"
                        : result.regime_gate.action === "DOWNGRADE" ? "rgba(245,158,11,0.35)"
                        : "rgba(0,255,136,0.3)"}`,
              borderRadius: 10, padding: "12px 14px", marginBottom: 12,
            }}>
              <div style={{
                fontSize: 9, letterSpacing: 2, marginBottom: 8, fontWeight: 700,
                color: result.regime_gate.action === "BLOCK" ? "#ff2d55"
                     : result.regime_gate.action === "DOWNGRADE" ? "#f59e0b" : "#00ff88",
              }}>
                ◆ REGIME GATE — {result.regime_gate.verdict} ({result.regime_gate.score}%)
              </div>
              <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 6 }}>
                {result.regime_gate.checks.map((c, i) => (
                  <div key={i} style={{ fontSize: 10, color: "#a0aec0", display: "flex", gap: 6, alignItems: "flex-start" }}>
                    <span style={{ color: c.pass ? "#00ff88" : "#ff2d55", flexShrink: 0 }}>{c.pass ? "✓" : "✗"}</span>
                    <div>
                      <div style={{ color: "#e8e8f0", fontWeight: 600 }}>{c.name}</div>
                      <div style={{ color: "#6b7a99", fontSize: 9 }}>{c.detail}</div>
                    </div>
                  </div>
                ))}
              </div>
              {result.gate_status && (
                <div style={{ fontSize: 10, color: "#a0aec0", marginTop: 10, paddingTop: 10, borderTop: "1px solid rgba(255,255,255,0.05)" }}>
                  {result.gate_status}
                </div>
              )}
            </div>
          )}
