<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>CLVRQuant App Icon</title>

<style>

/* ===== CANVAS ===== */
body {
    margin: 0;
    height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
    background: #111;
    font-family: Arial, sans-serif;
}

.icon {
    width: 1024px;
    height: 1024px;
    border-radius: 220px;
    position: relative;
    overflow: hidden;

    /* Deep fintech navy gradient */
    background: linear-gradient(
        135deg,
        #05070C,
        #0A0F1C
    );
}

/* ===== STAR CORE ===== */
.star {
    position: absolute;
    top: 50%;
    left: 50%;
    width: 520px;
    height: 520px;
    transform: translate(-50%, -50%);
}

/* Vertical spike */
.star::before {
    content: "";
    position: absolute;
    left: 50%;
    top: 0;
    width: 60px;
    height: 100%;
    transform: translateX(-50%);
    background: linear-gradient(
        to bottom,
        #FFE7A3,
        #D4AF37,
        #8C6A1D
    );
    clip-path: polygon(50% 0%, 100% 50%, 50% 100%, 0% 50%);
    filter: drop-shadow(0 0 60px rgba(246,216,120,0.25));
}

/* Horizontal spike */
.star::after {
    content: "";
    position: absolute;
    top: 50%;
    left: 0;
    width: 100%;
    height: 60px;
    transform: translateY(-50%);
    background: linear-gradient(
        to right,
        #FFE7A3,
        #D4AF37,
        #8C6A1D
    );
    clip-path: polygon(0% 50%, 50% 0%, 100% 50%, 50% 100%);
    filter: drop-shadow(0 0 60px rgba(246,216,120,0.25));
}

/* Center glow */
.core {
    position: absolute;
    top: 50%;
    left: 50%;
    width: 70px;
    height: 70px;
    transform: translate(-50%, -50%);
    border-radius: 50%;
    background: #FFE7A3;
    box-shadow:
        0 0 30px rgba(255,231,163,0.8),
        0 0 80px rgba(246,216,120,0.3);
}

/* ===== CANDLESTICKS ===== */

.candle {
    position: absolute;
    bottom: 360px;
    width: 14px;
    border-radius: 4px;
}

/* bearish */
.red {
    height: 70px;
    background: #FF4D6D;
    left: 260px;
    box-shadow: 0 0 12px rgba(255,77,109,0.5);
}

/* bullish small */
.green1 {
    height: 90px;
    background: #1ED760;
    right: 300px;
    box-shadow: 0 0 12px rgba(30,215,96,0.5);
}

/* bullish larger */
.green2 {
    height: 120px;
    background: #1ED760;
    right: 250px;
    box-shadow: 0 0 16px rgba(30,215,96,0.6);
}

/* subtle ambient glow */
.icon::after {
    content: "";
    position: absolute;
    inset: 0;
    background: radial-gradient(
        circle at center,
        rgba(246,216,120,0.08),
        transparent 70%
    );
}

</style>
</head>

<body>

<div class="icon">
    <div class="star">
        <div class="core"></div>
    </div>

    <div class="candle red"></div>
    <div class="candle green1"></div>
    <div class="candle green2"></div>
</div>

</body>
</html>